"""Evaluators and probes.

Every evaluator scores a language model through the interface in
``evals.backends`` (``LanguageModel``); the released checkpoints load through
the ``gptbert`` backend. Run the command-line entry points from the
repository root.
"""
